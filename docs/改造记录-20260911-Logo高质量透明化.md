# 改造记录：Logo 高质量透明化

## 修改了什么

- 重新使用原始 `new_logo.png` 处理 Logo。
- 原始文件已备份为 `docs/new_logo-original.png`，保留后续修改所需的完整素材。
- 改为仅对与画布外侧连通的浅色低饱和背景做透明处理，不再按全图亮度误删 Logo 内部浅色内容。
- 使用双线性采样缩放，降低缩放锯齿并改善边缘平滑度。
- 自动裁剪外围空白，按比例将主体缩放到约 94% 画布范围，不变形。
- 重新生成全部应用 Logo、PWA 图标和 favicon 资源。
- 更新 Service Worker 应用版本，确保旧图标缓存失效。

## 涉及文件

`docs/new_logo-original.png`、`assets/logo.svg`、`assets/logo-512.png`、`assets/logo-192.png`、`assets/logo-180.png`、`assets/logo-128.png`、`assets/logo-72.png`、`assets/logo-48.png`、`assets/favicon-32.png`、`assets/favicon-16.png`、`js/app-version.js`。

## 测试情况

- PNG 输出为 RGBA 透明格式。
- 构建通过。
- Git 引擎 35 项测试通过。
- Worker API 13 项测试通过。
- JavaScript 语法检查通过。
- `git diff --check` 通过。

## 遗留问题

根目录原始 `new_logo.png` 会在提交前删除，正式备份保存在 `docs/new_logo-original.png`。
