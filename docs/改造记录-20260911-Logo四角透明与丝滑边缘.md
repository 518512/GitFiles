# 改造记录：Logo 四角透明与丝滑边缘

## 修改了什么

- 使用最新上传的根目录 PNG 重新处理 Logo。
- 原图备份为 `docs/new_logo-source-2.png`。
- 将四角及外围背景设为透明，保留 Logo 主体区域。
- 对主体边缘使用双线性采样，减少缩放锯齿并改善小尺寸图标观感。
- 保持宽高比，主体按比例铺满画布，不发生变形。
- 重新生成全套 Logo、PWA 图标和 favicon。
- 更新应用版本，触发 Service Worker 淘汰旧图标缓存。

## 测试情况

- PNG 输出为 RGBA 格式。
- 构建成功。
- Git 引擎 35 项测试通过。
- Worker API 13 项测试通过。
- JavaScript 语法检查和 `git diff --check` 通过。

## 遗留问题

原始图片是 RGB 格式，没有独立 Alpha 通道；本次基于外围背景分割与边缘采样处理，已避免全图亮度阈值导致的主体浅色误删。
