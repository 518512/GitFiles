# 改造记录：GitHub 登录报错文案修正

- 日期：2026-09-08
- 关联：`docs/改造记录-20260908-登录页GitHub按钮.md`

## 修改了什么

`js/githubdisk.js` 的 `ensureConfigured()` 报错文案：

- "Cloudflare Pages build variables" → "CONFIG_GITHUB_CLIENT_ID build variable"
  （部署载体已是 Workers，且直接给出变量名）
- 章节引用改为 README 实际存在的「Configuring Client IDs」

## 为什么改

用户在首页点击 GitHub 登录后看到该报错；旧文案指向不存在的章节名与过时的
Pages 术语，误导排查方向（实际根因是用户线上部署为旧版本且未配置构建变量）。

## 涉及文件

- `js/githubdisk.js`、`js/app-version.js`

## 测试情况

`node --check` 通过；引擎回归 32/32。

## 遗留问题

用户侧需按 README 方式一重新完成 Fork → 连接 → 配置 → 部署。
