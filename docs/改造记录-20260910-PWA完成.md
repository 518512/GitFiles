# 改造记录：PWA 完成与加固

## 修改了什么

- 修复缓存清理入口错误：`StorageHubCache` 更正为 `GitFilesCache`。
- 完善 Manifest 的 `start_url` 和 `scope`，补充 iOS 独立应用元数据。
- 保持 Service Worker 应用壳缓存、Network First 页面策略、Cache First 静态资源策略和旧缓存清理。
- 明确 `/api/*` 不缓存，GitHub 文件与 OAuth 请求始终需要网络，避免离线假成功。
- 增加在线/离线状态提示，离线时明确本地存储可用范围。
- 增加 PWA 验收要求到项目规格文档。

## 涉及文件

`manifest.webmanifest`、`sw.js`、`js/register-sw.js`、`js/app.js`、`index.html`、`404.html`、`notepad.html`、`docs/PROJECT_SPEC.md`。

## 测试情况

- 前端和 Worker 相关脚本语法检查通过。
- Git 引擎测试 35 项通过。
- Worker API 测试 13 项通过。
- `node scripts/build-config.mjs` 构建通过。
- `git diff --check` 通过。

## 遗留问题

- Android/Chrome 的安装按钮和断网安装流程仍需在真实浏览器设备上手工验收；代码已满足 Manifest、Service Worker 和安全缓存策略要求。
