# 改造记录：紧凑添加存储按钮

## 修改了什么

- 调整桌面端顶部“添加存储”按钮为紧凑尺寸：约 92px 宽、32px 高、13px 字号。
- 移动端保留 44px 最小触控宽度和 40px 高度，仅显示加号，隐藏文字标签。
- 保留按钮标题和可见文字/图标，不改变添加存储事件和菜单逻辑。

## 为什么改

原按钮固定 106px 宽、40px 高，在统一工具栏中形成明显偏大的蓝色色块，桌面和移动端比例都不协调。

## 涉及文件

`css/ui-v2.css`。

## 测试情况

- 构建检查。
- `git diff --check`。
