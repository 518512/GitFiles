# GitFiles 项目状态总览

> 本文档是**项目状态的唯一权威来源**。完成情况、待办事项一律以本文为准。
>
> 最后更新：2026-09-18 · 对应 `main` 工作区（审阅修复阶段一至四，见
> `docs/改造记录-20260918-审阅修复性能与死代码.md`）

---

## 0. 如何使用本文档

| 文档 | 职责 |
|---|---|
| **本文 `docs/状态总览-20260912.md`** | 唯一状态来源：已完成 / 待规划 |
| `docs/架构现状-20260912.md` | 架构现状与上游关系定位 |
| `docs/PROJECT_SPEC.md` | 架构规格与契约（应当实现成什么样） |
| `AGENTS.md` | 开发规则与红线 |
| `docs/README.md` | 改造记录索引（按主题分组） |
| `docs/archive/` | 已过时的历史报告（仅供追溯） |

**维护规则**：完成一项工作后，在对应任务行把状态改为 ✅，并附上改造记录链接；
新出现的问题直接加到第 3 节，不要新建孤立的任务单文档。

---

## 1. 当前基线

> ⚠️ **提交哈希说明（2026-09-18）**：为统一提交身份，**我们自己的提交**（非上游）的作者/
> 提交者已改写为 `MbAIGC <MbAIGC@users.noreply.github.com>`；**上游原作者
> `mykhailo.mikus` 的 17 条提交已还原为原始署名**（SHA 与上游 `main` 完全一致）。
> 因此本仓库**我们自己的提交 SHA 全部变化**（内容与提交顺序不变，tree 哈希一致）。
> 文档中在此之前记录的具体 SHA 不再对应现有提交；历史重写前的快照如需取回，
> 可用旧 SHA 向 GitHub 索取。

```text
main（工作区）：审阅修复阶段一至四 + 后续清理批次 + 两个 bug 修复已实施，见
  docs/改造记录-20260918-审阅修复性能与死代码.md
  docs/改造记录-20260918-退出登录保留挂载.md
相对 upstream/fi3ik-mme/storage-hub：领先 70+ 个提交，落后 0 个
测试：node --test tests/worker-api.test.mjs tests/markdown-lite.test.mjs \
        tests/github-tree-cache.test.mjs tests/githubdisk-utils.test.mjs \
        tests/app-session.test.mjs
      → 97 项通过（worker 57 + markdown 15 + 树缓存 6 + 客户端工具 14 + 会话 5）
UI 结构校验：node scripts/check-ui.mjs → 0 error / 0 warning
CSS 审计：node scripts/audit-css.mjs --strict → 未处理 0 条
```

---

## 2. 已完成

### 2.1 核心架构（相对上游的改造）

| 能力 | 状态 | 说明 |
|---|---|---|
| Worker + Static Assets 后端 | ✅ | `workers/entry.js` 为唯一 HTTP 入口，`env.ASSETS` 回退静态资源 |
| D1 session + HttpOnly Cookie | ✅ | token 只在 Worker/D1，浏览器无 token；`workers/session.js` |
| 仓库 ACL（含 TTL 重校验） | ✅ | 读 5 分钟 TTL；**写操作每次强制回源 GitHub** |
| GitHub OAuth + PKCE | ✅ | popup 回调三通道交付；投递 origin 白名单 |
| Git Data 变更管线 | ✅ | 完整快照建 root tree（不用 `base_tree`，保证删除语义） |
| CAS 并发控制 | ✅ | `expectedHead` 比对，不一致 409 + `details{expectedHead,remoteHead}` |
| 批量 = 一个 Tree + 一个 Commit | ✅ | 含跨仓库复制、本地→GitHub 批量 |
| 空仓库首次写入 | ✅ | `expectedHead: null`，并发首建 ref 返回 409 |
| Conflict Center（基础） | ✅ | 记录冲突 + 重新加载远端状态 |
| PWA | ✅ | manifest / SW / 独立显示；`/api/*` 永不缓存 |

### 2.2 代码审阅问题（`docs/改造记录-20260912-代码审阅修复与工程护栏.md`）

| 编号 | 问题 | 状态 |
|---|---|---|
| H1 | OAuth 授权码可被投递到 `state` 内嵌的任意 origin | ✅ |
| H2/H5 | 文件下载整包进内存、无 Range | ✅ 流式透传 + 206 + 95MB 上限 |
| H3 | ACL 缓存永不刷新，撤权后仍可写 | ✅ TTL + 写操作强制重校 + `?refresh=1` |
| H4 | 非法 UTF-16 内容 → 500 且静默替换为 U+FFFD | ✅ 返回 422 |
| M1 | delete 不存在路径静默成功 | ✅ 返回 `missingPaths` |
| M2 | 超大仓库 tree 截断导致完全无法写入 | ✅ 惰性子树遍历 |
| M3 | 批量 blob 串行上传 | ✅ 8 路有界并发 |
| M4 | session 过期无清理 | ✅ 顺带清理 + schema 幂等 |
| M5 | 429 丢失 `Retry-After` | ✅ 透传 |
| M6 | 路径未做 Unicode 规范化 | ✅ NFC + 兼容历史 NFD 路径 |
| L1/L3 | 测试徽章失真、死代码 `githubRaw` | ✅ |

### 2.3 UI

| 工作 | 状态 | 说明 |
|---|---|---|
| CSS 层叠顺序反转 | ✅ | `docs/改造记录-20260912-代码审阅修复与工程护栏.md` 第二节；这是历次 V2 改动反复回退的根因 |
| `ui-v2.css` 重复块清理 | ✅ | 原有 3 个 `.ribbon` 块 + 2 个重叠媒体查询 |
| 移动端不再隐藏关键控件 | ✅ | 两行独立布局；触达目标 ≥44px |
| 首页重构为工作区入口 | ✅ | `docs/改造记录-20260912-首页图标与账户.md` |
| 登录首页改为落地页风格 | ✅ | 参照 deepseek.com 的极简落地页（浅底 + 居中大标题 + 胶囊按钮，去掉对话输入框等无关模块）；删除三代历史登录样式。见 `docs/改造记录-20260918-登录页落地页改版.md`。**待真机截图验证**（P-11） |
| 最近访问 | ✅ | 本地记录，无额外 API 请求 |
| 零状态主行动按钮 | ✅ | 内容区「添加存储」 |
| 侧栏「概览/仓库」语义合并 | ✅ | 移除 `overviewMode` |
| 空目录空态带动作 | ✅ | 上传 / 新建文件 |
| README 视图（含安全渲染器） | ✅ | 新增 `js/markdown-lite.js`，15 项 XSS 测试 |
| 顶栏刷新 loading 反馈 | ✅ | 禁用 + 旋转，尊重 reduced-motion |
| 面板层图标体系统一 | ✅ | `panelIcon()` 内联 SVG 集 |
| UI 结构静态校验 | ✅ | 新增 `scripts/check-ui.mjs` |
| CSS 单文件化 | ✅ | `css/ui-v2.css` 并入 `css/style.css`（`V2_LAYER_START` 标记切分两层），全部引用与校验脚本同步，见 `docs/改造记录-20260918-CSS单文件化与Windows风格清理.md` |
| Windows 风格清理 | ✅ | `--win-*` 令牌、`#0078d4` 进度条渐变、privacy/terms 主题色全部移除，统一 GitHub 风格 |
| 死代码清理（CSS） | ✅ | 删除无引用类 `local-drive-icon` / `app-dialog-perms`；保留 JS 动态拼接的 `file-item--pending-*` 等 |

### 2.4 早期审查任务单（`docs/archive/`）

来源：`代码审查-20260908-main系统审查与修复任务单.md`（上游时代，多数已过时）

| 任务 | 状态 | 结论依据 |
|---|---|---|
| T1 OAuth token 会话缓存（去重弹窗） | ✅ 已失效 | token 已移出浏览器，Worker session 取代 |
| T2 仓库生命周期重设计 | ✅ 已完成 | 「Add Repository」先列 ACL 仓库供选择 |
| T3 GitHub App token 过期处理 | ✅ 已失效 | 改为 Worker session，过期返回 401 引导重登 |
| T4 跨仓库/跨存储批量化 | ✅ 已完成 | `collectGithubItems` + `createBatchFromCollected`，目标仓库单 commit |
| T5 GitHub Pages 部署收口 | ✅ 已完成 | `pages.yml` 已含 build 步骤，且只上传 `public/` |
| T6 Conflict Center 状态机升级 | ⏳ 待规划 | 见 P-01 |
| T7 认证架构收口（token 出 localStorage） | ✅ 已完成 | Worker + D1 + HttpOnly Cookie |
| P0-1 ~ P0-4、P1-1 ~ P1-6、P2-1 ~ P2-5 | ✅ 基本闭环 | 除 P1-5（冲突 UI）转入 P-01 |

### 2.5 性能与死代码审阅修复（2026-09-18）

来源：`docs/代码审阅-性能与死代码-20260918.md`；实施记录：
`docs/改造记录-20260918-审阅修复性能与死代码.md`。

| 工作 | 状态 | 说明 |
|---|---|---|
| 浏览器端仓库树缓存 | ✅ | `js/githubdisk.js` 按 `diskId+branch` 缓存 + in-flight 合并 + `byPath` 索引；命中条件是缓存 head === `disk.head`。此前**根本没有缓存**，每次读树都是完整递归请求（AGENTS §19 名存实亡） |
| 写操作刷新合并 | ✅ | `notifyListChange` 合并到 120ms 窗口，一次上传不再触发多轮 `refreshGithubFolderView` |
| 多文件上传单 commit | ✅ | 新增 `GithubDisk.createFilesFromBlobs`：一次 Tree + 一次 Commit（原为每文件一个 commit，违反 §3） |
| 写前不再用整树换 HEAD | ✅ | `invalidateRepoTree` 只清缓存、不清 `disk.head`；写后死赋值一并移除 |
| Worker 操作索引增量维护 | ✅ | `workers/operations.js` 去掉逐个操作的 `buildPrefixIndex` + Map/Set 全量重建 |
| Worker Blob 去重键 | ✅ | 字节数组只按长度分组 + `sameContent` 复核，避免 `join(',')` 造成上百 MB 临时字符串 |
| 仓库 ACL 批量落库 | ✅ | `/api/repos?refresh=1` 改用 `env.DB.batch()`（原为最多 1000 次串行 D1 往返） |
| 文本判定统一 | ✅ | 单一 `isTextFileMime`（修正 `.markdown` 被判成二进制）；客户端上传上限对齐 25MB |
| 复制二进制不再假成功 | ✅ | GitHub→本地复制二进制改为明确报错，不再静默写空文件 |
| 记事本未保存拦截 | ✅ | 独立页注册 `beforeunload` |
| 冲突态样式 | ✅ | 补 `file-item--pending-conflict` / `file-pending-badge--conflict`（此前 JS 生成类但 CSS 无规则） |
| 死代码清理 | ✅ 部分 | 删除 pending/move/delete 三套死轮询簇、4 个 b64 函数、8 个无调用方函数与导出、`app.js` 的 `if (false)` 块（约 120 行）、contextmenu 的 6 个死复制函数等；**并删除 `js/github/*.js` 历史引擎与 `tests/github-engine.test.mjs`（场景覆盖已迁移到 Worker 测试）**；`notepad.js` 内嵌分支与 `app.js` 其余 Drive 分支保留 |
| 静态闸门增强 | ✅ | `check-ui.mjs` 识别模板字符串动态类（0 warning）；`audit-css.mjs` 增加层内重复报告 |
| 事件委托与微优化 | ✅ | 文件列表逐项约 14 个监听器 → 容器级委托；`updateProgressBars` 改 Map；`escapeHtml` 改正则；配额 60s TTL |
| 二进制 base64 传输 | ✅ | 客户端发 base64 + `encoding`，Worker 校验后透传；上限两端一致 25MB（PROJECT_SPEC §4.1） |
| **修复：目录内无法新建/重命名** | ✅ | `workers/operations.js` 的 `taken()` 把「祖先只是目录前缀」误判为冲突，导致往已存在目录里 create/rename 一律 422；改为只有祖先为**条目**才冲突。新增 3 项回归测试 |
| **修复：退出登录丢失挂载（并删本地数据）** | ✅ | `signOutGithub()` 曾调用 `ejectAllDrives()`，而 `LocalDisk.removeDisk()` 内部还会 `deleteAllForDisk()`——「退出登录」等于清空本地存储数据。现改为只结束会话；挂载按账号保留（`GithubDisk.setActiveAccount/getVisibleDisks`），重新登录同一账号自动恢复，换账号不显示他人挂载。见 `docs/改造记录-20260918-退出登录保留挂载.md`。**待真机验证**（P-11） |

遗留（详见审阅文档第 8 节「未修复」表）：记事本文本模型、本地 IDB 事务、
`notepad.js` 内嵌分支、`app.js` 其余 Drive 分支、CSS 历史层层内重复合并、
浏览器/Android 真机验证（P-11）。

---

## 3. 待规划（Backlog）

按优先级排列。**P0 表示影响数据正确性或安全**。

### P-01 Conflict Center 状态机（原 T6 / P1-5）· P1

现状：只有「记录冲突 + 重新加载远端状态」。

待做：

```text
状态机：Clean / Dirty / Committing / Conflict / Error / Offline
base(common ancestor) / ours / theirs 三方对比
逐文件 diff
文本三方合并
显式覆盖（默认禁止）
跨仓库转移的可恢复状态（retry / cleanup / compensation）
```

依据：AGENTS.md §4、PROJECT_SPEC §6。

### P-02 GitHub App installation token · P1

现状：使用 OAuth **user token**，scope 为 `repo`（偏大）。
**refresh_token 静默续期已实现**（2026-09-12）：GitHub App 的 8 小时 token
过期后由 `requireSession` 自动换新，隔夜重开不再要求重新登录。

待做：

```text
GitHub App + installation token（仓库级授权）
session 主动吊销与管理界面
```

依据：PROJECT_SPEC §10、`docs/改造记录-20260912-代码审阅修复与工程护栏.md` 遗留项。

### P-03 死代码清理 · P2（2026-09-18 已清理一批）

已删除（2026-09-18，见 `docs/改造记录-20260918-审阅修复性能与死代码.md`）：

```text
js/githubdisk.js      pending/move/delete 三套服务端确认轮询簇 + 三个定时器表
                      4 个 b64* 函数 + 永不命中的 base64 读取分支
                      listTrash / isGithubFolder / makeUniqueCopyName / trashFile /
                      copyFile / moveFile / prefersPatSignIn / getTreeEntrySize / readJsonResponse
                      upsertDiskFromRepo 的重复对象键
js/app.js             if (false) 包裹的 Drive 用户侧栏渲染（约 120 行）+ USER_SECTIONS
                      findTreeFile 转发包装、同值三元
js/contextmenu.js     6 个跨存储复制死函数 + collectGoogleIntoBatch（含 download→upload 的旧实现）
js/operation-progress.js  只写不读的 ITEM_TRACKS
js/localdisk.js       移除 getReservedSize 无用导出
js/drive.js           被覆盖的 parseNotepadFilePath
css/style.css         5 行内重复选择器块；零引用令牌 --color-success/--space-5/--space-6
check-ui.mjs 告警     已从 10 条假阳性降为 0（脚本改为识别模板字符串动态类）
```

仍待清理：

```text
js/app.js / js/contextmenu.js  其余 Google Drive 分支（运行时不可达，但横跨路由与
                               上下文菜单，删除需浏览器回归；见审阅文档 §8）
js/auth.js / js/drive.js       Google Drive 兼容壳（上游遗留，按 PROJECT_SPEC §7 保留作崩溃防护）
css/style.css                  历史层内部 30 个选择器重复定义（audit-css 现已报告；
                               已删除 7 个层叠安全的冗余块）
```

已于 2026-09-18 删除：`js/github/*.js`（约 1146 行上游时代浏览器端 Git 引擎）
与 `tests/github-engine.test.mjs`。删除前已把其 §22 场景覆盖（目录 move/copy、
mkdir、混合与 100 条批处理、空操作跳过提交、串行语义、422 fast-forward 归类）
迁移到 `tests/worker-api.test.mjs`，并另建 `tests/githubdisk-utils.test.mjs`
覆盖 `js/github-paths.js` 的路径助手。

### P-04 完整 UI V2 信息架构 · P2

依据 `docs/GitFiles UI V2 重构任务单.md`。已完成的三个结构性问题见 2.3；
**其余大部分尚未实施**：

```text
Repository Picker V2（独立页面式选择器）        未实现
Branch Selector                                未实现
Settings 页面                                  未实现
Mobile Bottom Navigation                       未实现
Sidebar 应用导航与目录树彻底分层               部分
登录页 V2                                      部分
Dialog / Context Menu V2                       部分
Design System 完整化（断点、动画、状态规范）   部分
```

### P-05 仓库工作区增强 · P2

```text
文件列表增加「提交信息 / SHA」列
  为每个文件取时间需要 commits?path=，一个目录 = N 次 API 调用，
  违反 AGENTS.md §27。正确做法是 Worker 侧批量解析 commit tree。
gitignore 语义 / 忽略文件显示
```

### P-06 文本三方合并 · P2

依赖 P-01。见 PROJECT_SPEC §6。

### P-07 真实端到端部署验证 · P1

```text
真实 D1 + Worker 部署下的 ACL 迁移（checked_at 列）验证
Android ↔ NAS ↔ GitHub 多设备场景实测
并发首建 ref、CAS 409 的线上验证
```

依据：AGENTS.md §22–24 场景矩阵、PROJECT_SPEC §12。

### P-08 单文件下载 95MB 上限 · P3

超过 95MB 需要 R2 中转才能真正支持。当前返回 413。

### P-09 本地存储二进制支持 · P3

`LocalDisk` 仅存文本；空态上传二进制会明确拒绝。需要 IndexedDB Blob 存储。

### P-10 MarkdownLite 能力补全 · P3

当前有意做小：不支持表格、任务列表、嵌套列表、自动链接。

取舍理由：安全优先，宁可少渲染也不放开原始 HTML。
若需完整 GFM，需引入经审计的解析器（AGENTS.md §29 要求先论证必要性）。

### P-11 UI 真机验证 · P1

本轮所有 UI 工作只做了静态结构与逻辑校验，**未做浏览器/Android 截图验证**。
需要确认：首页视觉、README 排版、44px 触达、移动端两行布局、
Android Chrome 安全区与地址栏收缩。

补（2026-09-18）：**登录 / 落地页改版**与**文件列表事件委托**同样只在静态层面校验过，
需要一并真机确认：

```text
登录页   桌面/窄屏标题换行、按钮竖排、导航链接触达、iOS 安全区、首屏光晕强度
文件列表 单击选中、双击打开、长按菜单、右键菜单、拖放移动、多选
```

---

## 4. 上游关系

见 `docs/架构现状-20260912.md` 第 3 节。

> ⚠️ 更正（2026-09-18）：此前本节写「已于 2026-09-12 脱离 fork 网络」，与
> `AGENTS.md §33` 的状态核实矛盾。按 `AGENTS.md` 的口径：
> `https://api.github.com/repos/MbAIGC/GitFiles` 目前仍返回 `"fork": true` 且带
> `parent`，**尚未真正脱离 fork 网络**。脱离需要在 GitHub 网页操作
> （Settings → General → Danger Zone → Leave fork network，不可逆）；
> 未完成前文档不得宣称"已脱离"。

项目按独立仓库维护，源自 [storage-hub](https://github.com/fi3ik-mme/storage-hub)
（作者 Mykhailo Mikus），README 顶部设有「致谢与致敬」专节。

⚠️ 上游未声明 LICENSE，取得授权前不可再分发。

---

## 5. 文档维护

- 新增改动一律写新的 `docs/改造记录-YYYYMMDD-主题.md`，**不追加到旧文件**（AGENTS.md §33）。
- 完成 backlog 项时同步更新本文第 2、3 节。
- 历史改造记录只做索引（`docs/README.md`），不改写内容。
