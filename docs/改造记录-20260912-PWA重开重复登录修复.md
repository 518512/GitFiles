# 改造记录 20260912 修复 PWA 每次重开都要求重新登录

## 问题

用户真机反馈：**PWA 每次重新打开都要重新认证登录**。

## 排查过程（含一次误判）

第一轮我判断为「/api/* 响应缺 Cache-Control」，加了 `no-store`——
**这个修复有价值但不是根因**。本轮重新读代码，在一份旧记录里找到决定性证据：

`改造记录-20260908-GitHubApp过期token与重复授权.md` 明确记载：
**用户的 Client ID 是 GitHub App（Ov23 前缀），user access token 默认 8 小时过期**。

而 `workers/entry.js` 的会话寿命计算：

```js
const expiresAt = Date.now() + Number(tokenPayload.expires_in || 60*60*24*7) * 1000;
```

GitHub App 的交换响应里 `expires_in` ≈ 28800（8 小时），同时返回一个
**约 6 个月有效的 refresh_token** —— 被代码完全忽略了。

于是：

```text
登录 → D1 session 8 小时后过期（Cookie 虽是 7 天，但 /api/me 校验的是 D1 行）
     ↓
purgeExpiredSessions 把过期 session 与 ACL 一并删除
     ↓
隔夜重开 PWA → /api/me 401 → 登录页 → 重新走一遍 OAuth
```

「每次重新打开都要重新认证」与「隔夜重开」完全吻合。8 小时是一个
"当天看不出来、隔天必然复现"的周期，这也是它拖到现在才暴露的原因。

> 复盘：上一轮我修了缓存就收手，没有追问"为什么 /api/me 会 401"。
> 而答案其实躺在一周前的改造记录里。排查应先翻历史，再假设新问题。

## 修复：refresh_token 静默续期

### 1. 登录时把续期凭据落库

`sessions` 新增可选列：`refresh_token` / `refresh_expires_at` / `client_id`
（续期时 GitHub 要求 client_id，落库避免为此新增运行时配置）。

### 2. `requireSession` 过期时静默续期

```text
session 过期且有未过期的 refresh_token
  → POST github.com/login/oauth/access_token (grant_type=refresh_token)
  → 新 access_token / expires_at / 轮换后的 refresh_token 写回 D1
  → 请求继续，用户无感
无 refresh_token 或续期失败 → 如实 401
```

每个 API 路由都经过 `requireSession`，所以续期覆盖所有请求路径。

### 3. Cookie 寿命对齐

登录 Cookie 由固定 7 天改为「有 refresh 有效期则最长 180 天」。
否则 Cookie 先过期，会把已续期的会话重新打回登录页。

### 4. 列内省重构（连带修正）

新增三个可选列后，原来逐属性的 try/catch 级联难以为继，改为
**`PRAGMA table_info` 内省真实列名 + 动态拼语句**（每 isolate 一次）。
这顺带消除了上次"SELECT 有回退、INSERT 没有"这类不对称缺陷的土壤。

同时修正内省失败时的兜底：从「假设全部列存在」改为**最保守的旧列集合**
——保证核心登录路径可用，新列缺失只是功能降级。

### 5. 已知取舍

- GitHub 会**轮换** refresh_token（每次续期返回新值）。新值必须立即落库；
  并发请求同时续期存在极小的竞态窗口（旧 token 已被用掉），
  表现为偶发一次 401，重新登录即可。个人使用场景可接受，已记录。
- 未实现 installation token 与主动吊销（仍属 P-02）。

## 涉及文件

```text
workers/session.js     PRAGMA 列内省；refreshAccessToken；requireSession 过期续期
workers/entry.js       登录落库 refresh 凭据与 client_id；Cookie 寿命对齐
workers/schema.sql     sessions 增加三个可选列（含用途注释）
AGENTS.md              §10 更新为「refresh 已实现」
docs/状态总览-20260912.md   P-02 标注部分完成
tests/worker-api.test.mjs   重写可选列测试（PRAGMA mock）+ 3 项续期测试
```

## 测试情况

```bash
node --test tests/github-engine.test.mjs tests/worker-api.test.mjs tests/markdown-lite.test.mjs
# pass 51 / fail 0（worker-api 增至 33）
node tests/github-engine.test.mjs
# 35 passed, 0 failed
node scripts/check-ui.mjs        # 0 error
node scripts/audit-css.mjs --strict   # 未处理 0 条
```

新增的续期测试覆盖：过期 + 有效 refresh → 200 且新 token 落库（含轮换）；
过期 + 无 refresh → 401；refresh 接口失败 → 401 不伪装成功。

## 补充修正：真正的根因是 Cookie 少了一个分号

用户澄清：**不是隔夜**，而是**关闭 PWA 后立即重开也会要求重新登录**。
这直接推翻了 8 小时理论——会话即使刚建立也失效，说明 **Cookie 本身没有落盘**。

重新逐字符核对 `Set-Cookie` 的拼接结果：

```text
实际生成: gitfiles_session=…; Path=/; HttpOnly; Secure; SameSite=Lax Max-Age=604800
                                                          ↑ 这里没有 ";"
```

`cookieAttributes()` 返回的字符串**末尾没有分号**，拼接 `${cookieAttributes} Max-Age=…`
时变成了 `SameSite=Lax Max-Age=604800`。浏览器按 `;` 拆分 Cookie 属性，
这一段被解析成**一个非法属性**（SameSite 的值混入了 "Max-Age=604800"）：

- 非法的 SameSite 值被浏览器忽略（回退默认值，Chrome 无感）
- **`Max-Age` 从未被解析** → Cookie 没有 `Max-Age` 也没有 `Expires`
  → **退化为会话级 Cookie：浏览器 / PWA 一关闭即被清除**

这才是「每次关闭重开都要重新登录」的**完整根因**，与 token 过期、缓存都无关——
哪怕刚登录完就关掉重开，Cookie 也已经没了。前两轮的修复（8 小时续期、
Cache-Control）各自有价值，但都不是这个现象的根因。

**修复**：`cookieAttributes()` 末尾补分号。并新增回归测试，
按 `;` 逐项解析 Set-Cookie，断言 `Max-Age` / `SameSite` / `HttpOnly` / `Secure`
各自是独立且合法的属性（含 http 与 https 两种协议分支），以及注销 Cookie 同样成立。

> 复盘：这个 bug 藏在最核心的一行模板字符串里。前两轮我分别从缓存和 token
> 过期入手，都没有逐字符核对最终生成的 `Set-Cookie` 头——
> 而用户描述的"秒级重开也失效"本来就是最直接的线索。

### 连带确认（review 中排除的项）

- `sw.js` 不拦截 `/api/*`；`CACHE_NAME` 版本化、activate 时清旧缓存 → 无缓存残留问题
- `handleItemTap` 设备分发、`selectFile` 增量选中、操作条更新 → 逻辑完整
- 空目录但可返回上级时，网格/列表正常显示 `..` 行，不会被空态遮盖
- `showLogin` 只切界面，不清除任何状态 → 无"重开即清登录态"的代码路径
- `expires_at` 单位一致（毫秒）、`purgeExpiredSessions` 只删已过期行 → 无误删

## 你需要做的一件事

**重新部署后，重新登录一次。** 这是最后一次手动登录——

- 新登录会写入 refresh_token，此后 8 小时的 token 过期会被自动续期
- 登录时执行这两条可选的 ALTER 可以补齐旧库列（不执行也能用，只是
  续期凭据存不进旧库，隔夜重开仍会要求登录）：

```sql
ALTER TABLE sessions ADD COLUMN refresh_token TEXT;
ALTER TABLE sessions ADD COLUMN refresh_expires_at INTEGER;
ALTER TABLE sessions ADD COLUMN client_id TEXT;
```

> 必须重新登录一次的原因：旧 session 行里没有 refresh_token，
> 修复无法为它续期。

## 遗留问题

- refresh 轮换的并发竞态（见上），如出现偶发 401 重新登录即可。
- 修复后 Cookie 恢复 7 天有效期；若仍出现"关闭重开即掉登录"，
  请检查浏览器是否开启了"关闭时清除 Cookie/站点数据"（部分浏览器的隐私设置）。
- installation token 与吊销管理仍属 P-02。
- 本环境无法对真实 GitHub App 验证续期（测试 mock 了 GitHub 端点），
  真机表现需部署确认。
