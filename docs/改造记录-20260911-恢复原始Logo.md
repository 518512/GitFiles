# 改造记录：恢复原始 Logo

## 修改了什么

- 根据用户要求，将 Logo 和 favicon 资源恢复为仓库原始版本。
- 恢复 `logo.svg`、各尺寸 PNG 以及 16/32 像素 favicon。
- 更新应用版本号，确保 Service Worker 淘汰此前处理过的 Logo 缓存。
- 保留此前上传素材备份，不影响后续重新设计。

## 涉及文件

`assets/logo.svg`、`assets/logo-512.png`、`assets/logo-192.png`、`assets/logo-180.png`、`assets/logo-128.png`、`assets/logo-72.png`、`assets/logo-48.png`、`assets/favicon-32.png`、`assets/favicon-16.png`、`js/app-version.js`。

## 测试情况

- 资源文件格式检查通过。
- `git diff --check` 通过。
- 已更新 Service Worker 缓存版本。
